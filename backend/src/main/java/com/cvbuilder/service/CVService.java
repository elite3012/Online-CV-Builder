package com.cvbuilder.service;

import org.springframework.stereotype.Service;

/**
 * CV Service - Business logic for CV operations (UC3, UC4)
 * Includes ownership validation
 */
@Service
public class CVService {

    // Create CV
    // Get CV by ID (with ownership check)
    // Get all CVs for user
    // Update CV (with ownership check)
    // Delete CV (with ownership check)
    // Validate CV data (UC6, US12)
}
