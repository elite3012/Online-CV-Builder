package com.cvbuilder.service;

import org.springframework.stereotype.Service;

/**
 * Auth Service - Handles authentication logic (UC1, UC2, US1)
 * JWT token generation, password hashing, user validation
 */
@Service
public class AuthService {

    // Register user
    // Login user and generate JWT token
    // Validate JWT token
    // Logout (if needed, e.g., token blacklist)
}
